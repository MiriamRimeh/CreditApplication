@page
@model CreditApplication.Pages.ApplicationForm.Step2Model

@{
    ViewData["Title"] = "Адрес";
}

<div class="progress" role="progressbar" aria-label="Example with label" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100">
    <div class="progress-bar bg-warning" style="width: 50%">Стъпка 2</div>
</div>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header text-center">
                    <h2 class="mb-0">Данни за адрес</h2>
                </div>
                <div class="card-body">
                    <form method="post" class="row g-3 needs-validation" novalidate>
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <div class="form-group col-12">
                            <span class="text-danger">*</span>
                            <label asp-for="ClientAddress.City" class="control-label">Населено място:</label>
                            <input asp-for="ClientAddress.City" class="form-control" required="" />
                            <span asp-validation-for="ClientAddress.City" class="text-danger"></span>
                        </div>
                        <div class="form-group col-12">
                            <span class="text-danger">*</span>
                            <label asp-for="ClientAddress.StreetNeighbourhood" class="control-label">Улица/Жилищен Комплекс:</label>
                            <input asp-for="ClientAddress.StreetNeighbourhood" class="form-control" required="" />
                            <span asp-validation-for="ClientAddress.StreetNeighbourhood" class="text-danger"></span>
                        </div>
                        <div class="form-group col-12">
                            <label asp-for="ClientAddress.Number" class="control-label">Номер:</label>
                            <input asp-for="ClientAddress.Number" class="form-control" required="" />
                            <span asp-validation-for="ClientAddress.Number" class="text-danger"></span>
                        </div>
                        <div class="form-group col-12">
                            <span class="text-danger">*</span>
                            <label asp-for="ClientAddress.PostCode" class="control-label">Пощенски код:</label>
                            <input asp-for="ClientAddress.PostCode" class="form-control" required="" />
                            <span asp-validation-for="ClientAddress.PostCode" class="text-danger"></span>
                        </div>
                        <div class="col-12 text-center mt-4">
                            <button type="submit" class="btn btn-primary-miriam float-md-end">Напред</button>
                            <a asp-page="Step1_Personal" class="btn btn-outline-dark float-md-start">Назад</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
