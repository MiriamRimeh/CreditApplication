@page
@model CreditApplication.Pages.ClientAddresses.DeleteModel

@using System.Globalization

@{
    ViewData["Title"] = "Изтриване на адрес";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header text-center">
                    <h2 class="h4 mb-0">Изтриване на адрес</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger" role="alert">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                             class="bi bi-exclamation-octagon-fill" viewBox="0 0 16 16">
                            <path d="M11.46.146A.5.5 0 0 0 11.107 0H4.893a.5.5 0 0 0-.353.146L.146
                                     4.54A.5.5 0 0 0 0 4.893v6.214a.5.5 0 0 0 .146.353l4.394
                                     4.394a.5.5 0 0 0 .353.146h6.214a.5.5 0 0 0 .353-.146l4.394-4.394a.5.5
                                     0 0 0 .146-.353V4.893a.5.5 0 0 0-.146-.353zM8 4c.535 0 .954.462.9.995l-.35
                                     3.507a.552.552 0 0 1-1.1 0L7.1 4.995A.905.905 0 0 1 8 4m.002 6a1 1 0
                                     1 1 0 2 1 1 0 0 1 0-2" />
                        </svg>
                        Сигурни ли сте, че искате да изтриете този адрес?
                    </div>
                    <dl class="row g-3 mb-0">
                        <dt class="col-md-4">Номер на клиент</dt>
                        <dd class="col-md-8">@Model.ClientAddress?.ClientID</dd>

                        <dt class="col-md-4">ЕГН на клиент</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.Client?.EGN 
                        </dd>

                        <dt class="col-md-4">Населено място</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.City
                        </dd>

                        <dt class="col-md-4">Улица/Квартал</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.StreetNeighbourhood
                        </dd>

                        <dt class="col-md-4">Номер</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.Number
                        </dd>

                        <dt class="col-md-4">Пощенски код</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.PostCode
                        </dd>

                        <dt class="col-md-4">Създадено на</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.CreatedOn.ToString("dd.MM.yyyy HH:mm") 
                        </dd>

                        <dt class="col-md-4">Последна промяна</dt>
                        <dd class="col-md-8">
                            @Model.ClientAddress?.ModifiedOn.ToString("dd.MM.yyyy HH:mm") 
                        </dd>
                    </dl>
                </div>
                <div class="card-footer text-center">
                    <form method="post" class="d-inline">
                        <input type="hidden" asp-for="ClientAddress.ID" />
                        <button type="submit" class="btn btn-danger px-4 me-2">Изтрий</button>
                    </form>
                    <a asp-page="./Index" class="btn btn-secondary-miriam px-4">Назад</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
